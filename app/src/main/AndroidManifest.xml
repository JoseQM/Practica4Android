<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.practica4">


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_logo_plus"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_logo_plus_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SeleccionaImagenes"></activity>
        <activity android:name=".DesplazandoImagenes" />
        <activity
            android:name=".JuegoDeAciertos"
            android:label="@string/activityAcierto" />
        <activity
            android:name=".NumerosPrimos"
            android:label="@string/activityPrimos" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>